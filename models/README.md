# Model Artifacts

Do not commit large checkpoints to the repository.

Store locally or in an approved model registry:

- VLM adapters,
- change detector weights,
- optical–SAR fusion weights,
- calibration parameters,
- router classifier.

Every model artifact should have a metadata file recording source checkpoint, dataset, commit SHA, config and evaluation metrics.
